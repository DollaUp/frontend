import { css } from 'utils/styled-components';

export default css`
  h2.page-header {
    font-weight: 100;
    font-size: 30px;
    color: ${props => props.theme.main};
  }

  h3.section-header {
  }
`;
